DISCORD_TOKEN=your-bot-token-here
CLIENT_ID=your-application-client-id
# Optional: register slash commands to a single guild for instant updates (leave blank for global)
GUILD_ID=
# Optional: where JSON state is stored (default ./data)
DATA_DIR=./data
